{
  "name": "@tms/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo \"(config) skip\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "echo \"(config) ok\""
  },
  "dependencies": {
    "zod": "^4.1.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
