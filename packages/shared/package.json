{
  "name": "@tms/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo \"(shared) skip\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "echo \"(shared) ok\""
  },
  "dependencies": {
    "@tms/config": "workspace:*",
    "zod": "^4.1.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
